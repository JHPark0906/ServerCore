# 변경 기록

## 0.2.0 — 2026-09-23

웹·게임 백엔드가 공유하는 서버 통신·실행·운영 기능을 확장하고 C ABI와 Rust 사용 경계를 추가했습니다. 표준 라이브러리·컴파일러 런타임·운영체제 API 외의 라이브러리 의존성은 없습니다.

- HTTP 동적 경로, 비동기 정책·처리기, 요청·응답 스트리밍, SSE, 파일 Range·조건부 응답과 WebSocket 승인.
- 제한된 작업 실행·취소, TCP 송수신 흐름 제어, 정상 종료 대기와 마감 시간, 입력·인증·프레임 제한.
- TCP·UDP 바이너리 메시지, UDP 등록과 세션 수명 연결, 서버별 로거, 고정 메트릭·지연 분포·Prometheus 변환.
- Windows·Linux C ABI와 Rust의 HTTP·WebSocket·TCP 서버 API, 소유 handle과 Future. C ABI는 버전 1입니다.
- 공통 실행·전송 계층의 의존 경계를 정리하고 자동 include 계층 검사를 추가했습니다.

### 이전 시 확인할 사항

0.x 개발 단계에서는 minor 변경에 공개 API 변경이 포함됩니다. CMake 패키지는 같은 minor만 호환으로 판단하고, Rust 패키지도 0.2 계열을 명시합니다. 라이브러리와 소비 애플리케이션을 함께 다시 빌드합니다.

개발 중 추가했던 외부 HTTP 클라이언트와 libcurl 연결은 이번 배포 범위에서 제외했습니다. 해당 개발 API를 사용했다면 외부 HTTP 요청은 애플리케이션의 전송 구현으로 이전합니다. 나머지 이름 변경과 deprecated API는 [이전 안내](docs/API_MIGRATION.md)를 따릅니다. 서버 TLS는 배포 계층에서 처리합니다.

검증 환경과 결과는 [검증 기록](docs/VALIDATION.md)에 있습니다.
