#pragma once

namespace ServerCore::Core
{
/// <summary>
/// 계약 위반을 보고하고 프로세스를 즉시 끝낸다. 돌아오지 않는다.
/// </summary>
/// <remarks>
/// 왜 이 자리에 있는가:
/// 이 코어는 실패를 두 부류로 가른다. 와이어에서 온 깨진 입력 같은 예상되는 실패는 Status로
/// 돌려주고, 프로그래밍 오류인 계약 위반은 값으로 알리지 않는다. 계약 위반에 오류 값을 붙이면
/// 호출자가 자기 버그를 조용히 넘기게 되기 때문이다. 그 "값으로 알리지 않는다"를 실제로 하는
/// 것이 이 함수다.
///
/// 왜 매크로를 거쳐 부르는가:
/// 조건식의 원문과 파일 이름과 줄 번호는 그 자리에서만 잡을 수 있다. 이를 이 함수에 넘기는
/// 단언 매크로는 매크로 사용 금지 규약의 예외다.
///
/// 왜 전역 로거를 쓰지 않는가:
/// 로거는 부팅 첫 단계에서 설치되므로 그 전에 계약 위반이 나면 설치되어 있지 않다. 그리고
/// 설치 전의 기록은 사라진다고 로깅 층이 명시했다. 로거가 없어서 단언이 조용해지면 앞뒤가
/// 바뀐다. 그래서 표준 오류 스트림에 직접 쓴다.
///
/// 스레드 안전성: 스레드 안전. 어느 스레드에서 불려도 된다.
///
/// 약속하지 않는 것:
/// - 정리를 약속하지 않는다. 소멸자가 돌지 않고 열린 파일이 정상적으로 닫히지 않는다.
///   계약 위반은 프로그램의 가정이 이미 깨진 상태이므로 더 도는 것이 안전하지 않다.
/// - 출력이 어디까지 보이는지 약속하지 않는다. 표준 오류가 리디렉션되어 있으면 그쪽으로 간다.
/// </remarks>
/// <param name="expression">거짓이 된 조건식의 원문.</param>
/// <param name="file">그 조건식이 있는 파일.</param>
/// <param name="line">그 조건식이 있는 줄.</param>
/// <param name="message">무엇이 기대와 달랐는지. 원인 짐작을 담지 않는다.</param>
[[noreturn]] void ReportAssertFailure(
    const char* expression, const char* file, int line, const char* message) noexcept;
}

/// <summary>
/// 계약이 지켜지는지 확인하고, 아니면 그 자리에서 프로세스를 끝낸다.
/// </summary>
/// <remarks>
/// Debug와 Release 양쪽에서 살아 있다. NDEBUG에 걸리지 않는다.
///
/// 왜 Release에서도 켜 두는가:
/// Release가 프로덕션 구성이다. 거기서 사라지면 이 단언은 없는 것과 같다. 그리고 사라졌을 때의
/// 결과가 나쁜 쪽이다. 예를 들어 실패한 Result에 Value()를 부르면 기본 생성된 값이 조용히
/// 돌아온다. 크래시도 아니고 오류도 아니어서 아무도 이상하게 여기지 않는다.
/// 비용은 조건 분기 하나이며, 계약 위반을 즉시 드러내 사후 추적을 줄인다.
/// 성능 영향은 실제 부하에서 측정한다.
///
/// 무엇에 쓰는가:
/// 계약 위반에만 쓴다. 즉 호출자가 약속을 어긴 경우다. 와이어에서 온 잘못된 입력이나 끊긴
/// 연결처럼 정상적으로 예상되는 실패에는 쓰지 않는다. 그런 것은 Status로 돌려준다.
/// </remarks>
#define SERVERCORE_ASSERT(condition, message)                                                      \
    do                                                                                             \
    {                                                                                              \
        if (!(condition))                                                                          \
        {                                                                                          \
            ::ServerCore::Core::ReportAssertFailure(#condition, __FILE__, __LINE__, (message));    \
        }                                                                                          \
    } while (false)
