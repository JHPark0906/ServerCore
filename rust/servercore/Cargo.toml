[package]
name = "servercore"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Owned, asynchronous Rust handles for ServerCore HTTP, WebSocket and TCP"

[dependencies]
servercore-sys = { path = "../servercore-sys", version = "0.3.0" }
